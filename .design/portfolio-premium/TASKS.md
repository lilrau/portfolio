# Build Tasks: Portfolio Premium — Raul Souza Silva

Generated from: .design/portfolio-premium/DESIGN_BRIEF.md
Also: INFORMATION_ARCHITECTURE.md, DESIGN_TOKENS.md, `src/styles/tokens.css`
Date: 2026-09-22

**Filosofia:** *Liquid Editorial*. A task 1 estabelece a direção visual (tipografia + tokens) antes de tudo; as seguintes constroem sobre ela.

**Inventário atual e destino**
- Reusar: `src/styles/tokens.css`, `framer-motion`, `lucide-react` (ícones mínimos), `next/image`, os assets de projeto em `public/`
- Modificar: `components/fluid.tsx` + `public/fluid.js`, `components/LiquidGlass.tsx` → `GlassSurface`, `app/layout.tsx`, `app/globals.css`, `data/projects.json` → `data/projects.ts`
- Remover (task de limpeza): `components/CodeTerminal/*`, `components/Flex.tsx`, `components/Text.tsx`, `src/interfaces/`, `src/types/` (só servem ao terminal), deps `sass`, `liquid-glass-react`, `@specy/liquid-glass-react`, `package-lock.json`
- Convenções: componentes em PascalCase dentro de `src/components/` (seções em `src/components/sections/`), sem testes no projeto. A verificação é `next build` + lint + checagem visual no navegador.

## Foundation

- [x] **1. Hero no novo sistema tipográfico**: Instrument Serif carregada via `next/font` (`--font-instrument-serif`); Inter e o `@import` saem; `body` passa a usar `bg-canvas text-ink font-sans`. O hero é reescrito como componente `sections/Hero.tsx`: nome monumental (`text-hero`), cargo, proposta de valor com palavra em serifa itálica, CTAs "Get in touch" (#contact) e "Résumé" (PDF), micro-status "Currently at Amazoca · Remote, Brazil" e dica de scroll. Terminal e memoji saem da página. **Pronto quando:** o hero fica legível sobre o fluido nos dois temas a 375px e 1440px, sem scroll horizontal, e o resto da página antiga continua funcionando abaixo. _Reuses: tokens.css, FluidBackground. New: Hero._
- [x] **2. Fluido com a paleta curada**: `fluid.js` lê `--fluid-back`, `--fluid-color-1..5` e `--fluid-intensity` via `getComputedStyle` e relê quando mudam `data-theme` ou `prefers-color-scheme`; `generateColor()` sorteia do pool; `FluidBackground` recebe `aria-hidden`, pausa o loop com a aba oculta e sem splats automáticos em `prefers-reduced-motion` (continua reagindo ao ponteiro); script carregado depois do conteúdo (`next/script` com `lazyOnload`). A física não muda. **Pronto quando:** os splats saem só em tons índigo/violeta/ciano e a cor de fundo do canvas bate com `--color-bg-primary` nos dois temas. _Modifies: fluid.tsx, public/fluid.js._
- [x] **3. GlassSurface com refração real** (maior risco técnico): reescrever `LiquidGlass.tsx` como `GlassSurface` com filtro SVG (`feTurbulence`/mapa gerado + `feDisplacementMap`) aplicado via `backdrop-filter: url(#…)` no Chromium, realce especular (`--shadow-glass-surface`), tint por token e variantes `pill` / `panel` / `lens` / `strong`; nos outros navegadores cai para blur + saturate. Estado de press com `--easing-bounce`. Aplicar aos CTAs do hero. **Pronto quando:** no Chrome o fluido visivelmente distorce atrás do vidro; no Firefox/Safari aparece blur limpo sem artefato; o contraste do texto dentro do vidro bate com a tabela do DESIGN_TOKENS. _Modifies: LiquidGlass.tsx → GlassSurface.tsx._
- [x] **4. Tema: sistema por padrão, sem flash**: apagar `src/app/glass-lab/` (página temporária da task 3); script inline no `<head>` resolve `localStorage` → `prefers-color-scheme` antes do primeiro paint; `ThemeToggle` (ícone sol/lua em `GlassSurface` pill) alterna e persiste; o fluido faz crossfade do fundo em vez de trocar seco; os overrides legados de dark em `globals.css` continuam só até a limpeza. **Pronto quando:** recarregar em dark não pisca branco e mudar o tema do SO com a página aberta (sem escolha manual) atualiza site e fluido. _Modifies: layout.tsx, theme toggle em page.tsx. New: ThemeToggle, theme-script._
- [x] **5. Rotas bilíngues `/en` e `/pt`**: mover a página para `app/[locale]/` (layout com `lang`, `generateStaticParams`, metadata traduzida, `hreflang`/canonical); `proxy.ts` redireciona `/` por `Accept-Language`; dicionários tipados `src/i18n/en.ts` e `pt.ts` com os textos do hero; `LocaleSwitcher` (EN / PT) no canto fixo troca a rota preservando o `#hash`. **Pronto quando:** `/` em navegador pt-BR cai em `/pt` com o hero em português, o seletor leva a `/en#…` na mesma posição e o build pré-renderiza as duas rotas. _New: i18n/, proxy.ts, LocaleSwitcher, app/[locale]/._

## Core UI

- [x] **6. NavBar de vidro (desktop + mobile)**: pill `GlassSurface strong` com monograma "RS" (topo), Work · Experience · About · Contact, divisor, EN/PT, tema e Résumé; aparece depois do hero; seção ativa via IntersectionObserver. No mobile: monograma + "Contact" + botão de menu que abre uma sheet de vidro de baixo para cima (links em display, utilitários, e-mail), fechando com Esc, swipe ou toque fora, com foco preso. Substitui a nav antiga e os cards de navegação do hero. **Pronto quando:** a nav funciona por teclado e a sheet não deixa scroll do body vazar. _Depends on: 3, 5. Modifies: nav em page.tsx. New: NavBar, MobileSheet._
- [x] **7. Modelo de dados + cards de destaque (Work)**: `data/projects.ts` tipado (`slug`, `featured`, `order`, `year`, `role`, `impact[{value,label}]`, `achievements[]`, `description`, `tags`, `images`, `links`) com campos `{ en, pt }`; métricas do currículo (Amazoca: 175+ endpoints, −63% latência, 15→72% cobertura; AquiCob: 100+ operadores, ~40s 1ª resposta, +23% resposta; EduSaaS: multi-tenant, 60+ modelos; Hi: 47 modelos, build 8→3 min); tags do Hi corrigidas para NestJS/Prisma/React/Vite; campos sem dado marcados `// TODO(raul)`. Seção `sections/Work.tsx` com eyebrow + headline "Selected *work*" e 4 `FeaturedProjectCard` (imagem → nome → papel/ano → métricas em `text-metric` serifada → `TechTag` neutra). **Pronto quando:** os 4 cards renderizam nas duas línguas e nenhuma cor por tecnologia sobrou. _Depends on: 3, 5. New: projects.ts, Work, FeaturedProjectCard, ImpactStat, TechTag._
- [x] **8. Lista "More work"**: `ProjectList` com AI Receptionist, AI Debt Agent, FlyX Transport, MEI-U e BootStack (novo, com link do GitHub; TODO para a URL exata do repo): nome · tipo curto · ano · até 3 tags; no desktop, preview da imagem flutua junto ao cursor no hover; rodapé "More on GitHub" (substitui o card fake). **Pronto quando:** as linhas são focáveis e clicáveis e o preview não aparece em touch. _Depends on: 7. New: ProjectList._
- [x] **9. ProjectDetail (overlay com hash)**: abrir um card ou linha define `#work/<slug>` e mostra o painel de vidro `xl` sobre `--color-surface-overlay`: galeria (setas, teclado, swipe, contador), nome, papel, período, links, impacto, descrição, stack completo, anterior/próximo. Esc/X fecha, devolve o foco ao card e limpa o hash; carregar a URL com hash abre o painel direto. Substitui o modal antigo. **Pronto quando:** `/pt#work/amazoca` abre o Amazoca em PT num carregamento frio, e o foco fica preso no painel. _Depends on: 7. Modifies: modal em page.tsx → ProjectDetail._
- [x] **10. Experience**: `data/experience.ts` bilíngue; `sections/Experience.tsx` com timeline reversa (Amazoca com os dois cargos Junior → Full Stack, AquiCob, MoonRock co-founder, UTFPR × UNESP), período e local em mono, 2-3 conquistas com número em destaque; `EducationStrip` (UTFPR ADS, **English C1** destacado, Mackenzie AI); `ResumeButton` primário ("Currículo (PDF, em inglês)" em PT). Remove o iframe do PDF. **Pronto quando:** a timeline lê bem em coluna única a 375px e o PDF abre em nova aba. _New: Experience, EducationStrip, ResumeButton._
- [x] **11. About + Stack**: foto real (`dev_pic_blur_bg.png`) em moldura `GlassSurface panel`, parágrafo curto de apresentação, uma frase sobre o background de educador, idiomas (PT nativo · English C1) e o bloco Stack em 3 colunas tipográficas (Backend · Frontend · AI & Infra, conforme o currículo) sem logos. Substitui as seções About e Stack antigas. **Pronto quando:** não sobra nenhum ícone de CDN externo na página e o texto fica em ≤ `max-w-content`. _Reuses: GlassSurface. New: About, StackBlock._
- [x] **12. Contact + Footer**: headline de fechamento grande com acento itálico, e-mail em destaque (`mailto`) com botão de copiar ("Copied"/"Copiado" por 2s, `aria-live`), LinkedIn e GitHub, "Brazil · UTC−3 · open to remote"; footer com © ano, links e "Back to top". Saem o Discord e o iPhone mockup. **Pronto quando:** copiar funciona e anuncia no leitor de tela, e a página inteira termina sem nenhuma seção legada. _New: Contact, Footer._

## Interactions & States (camada Awwwards)

- [x] **13. RevealHeadline (split text)**: componente que divide headlines em linhas/palavras com máscara e revela com `--easing-out` + `--stagger-line` ao entrar na viewport; texto completo em `aria-label`, spans `aria-hidden`; em reduced-motion vira fade simples. Aplicar no hero (sequência de entrada), em todas as seções e no Contact. Covers: entrada inicial, entrada por scroll, reduced-motion, redimensionamento (re-split sem quebrar). _Depends on: 1, 7–12._
- [x] **14. Scroll coreografado**: Lenis (desktop, fora de reduced-motion) sincronizado com o framer-motion; âncoras da nav e dos CTAs usam o scroll do Lenis (remove o `smoothScrollTo` manual); parallax leve nas imagens dos cards de destaque; entrada em stagger dos cards e das linhas da lista. Covers: âncora com offset da nav, hash de projeto não rola a página, mobile com scroll nativo, overlay aberto trava o Lenis. _Depends on: 6–9. New: SmoothScroll._
- [x] **15. GlassCursor**: lente `GlassSurface lens` (refração 70 e aberração cromática) que segue o ponteiro com mola; encolhe e ganha contorno sobre links e botões; expande com rótulo "View"/"Ver" sobre cards; esconde sobre inputs e texto selecionável; desligado em touch, reduced-motion e < `lg`; o movimento continua alimentando o fluido. Covers: default, link, card, oculto, saída da janela. _Depends on: 3. New: GlassCursor._
- [x] **16. Preloader**: primeira visita da sessão (sessionStorage): nome em reveal e uma lâmina de vidro que se dissolve para o fluido, com um splat automático inicial; ≤ `--duration-preloader`; clique ou tecla pula; nunca aparece com `#work/<slug>` na URL nem em reduced-motion; hero encadeia a entrada logo em seguida. Covers: primeira visita, retorno na sessão, deep link, reduced-motion. _Depends on: 1, 2, 13. New: Preloader._
- [x] **17. Transição card → detalhe**: shared layout do framer-motion (`layoutId`) da imagem e do título do card para o painel e de volta ao fechar; a lista usa uma transição de escala e fade a partir da linha. Covers: abrir, fechar, próximo/anterior dentro do painel, deep link (sem animação de origem). _Depends on: 9._

## Responsive & Polish

- [x] **18. 404, OG images e favicon**: `app/[locale]/not-found.tsx` com o fluido e um link para a home do idioma; `opengraph-image.tsx` por idioma (nome, cargo, proposta, fundo com a paleta do fluido) via `next/og`; favicon/ícones com o monograma "RS" no lugar do `memoji.ico`. **Pronto quando:** o preview do link no LinkedIn/WhatsApp (validador de OG) mostra a imagem certa por idioma. _New: not-found, opengraph-image, icon._
- [x] **19. Limpeza do legado**: apagar o `page.tsx` antigo e os componentes listados em "Remover"; tirar os overrides `.text-gray-*`/`.tech-tag` e `--background`/`--foreground` do `globals.css`; `pnpm remove sass liquid-glass-react @specy/liquid-glass-react`; remover `package-lock.json`; listar assets sem uso em `public/` (memoji, iPhone mockup, svgs do Next) e **confirmar com o Raul antes de apagar**. **Pronto quando:** `next build` e `eslint` passam sem warnings novos e `grep` não encontra referências aos arquivos removidos.
- [x] **20. Passe responsivo**: verificar e ajustar a 375, 768, 1024 e 1440px em light/dark: quebra do nome no hero, grid de destaques (1 → 2 col), timeline, overlay em tela cheia no mobile, sheet da nav, gutters ≥ 16px, touch targets ≥ 44px (atenção: os botões EN/PT do `LocaleSwitcher` têm 28px de altura dentro da pill de 40px), sem scroll horizontal (hoje garantido por `overflow-x: clip` em html/body — confirmar que nada fica cortado). Breakpoints: base, md, lg, xl.
- [x] **21. Passe de acessibilidade**: contraste AA conferido sobre o fluido nos dois temas (regras do DESIGN_TOKENS; atenção: no dark, splats claros atrás de texto sem vidro derrubam o contraste momentaneamente — avaliar um véu/scrim sutil atrás do hero ou limitar a luminância do dye), `:focus-visible` em todos os interativos, skip link, foco preso e devolvido em overlay e sheet, `lang` por rota, `alt` bilíngue nas imagens, canvas `aria-hidden`, split text lido como frase inteira, `prefers-reduced-motion` desligando preloader, Lenis, parallax, cursor e splats automáticos.
- [x] **22. Sanidade de performance** _(medido em headless sem GPU: CLS 0, FCP 44–220 ms, LCP 1.6 s na volta / ~3 s na 1ª visita por causa do preloader; o fluido em WebGL por software não é representativo — conferir com Lighthouse num Chrome real)_ (visual acima de tudo, mas sem travar): o fluido pausa com a aba oculta; o número de superfícies com refração SVG simultâneas fica limitado (as demais usam blur); imagens com `sizes` corretos e `priority` só no que aparece na primeira dobra; LCP do hero não depende do fluido nem do preloader. **Pronto quando:** não há jank perceptível ao rolar num notebook médio e o Lighthouse não aponta regressão grave de LCP/CLS.

## Review

- [x] **Design review**: rodar `/design-review` contra o brief (screenshots em `.design/portfolio-premium/screenshots/`).

> Revisão salva em `DESIGN_REVIEW.md` (2 must fix, 8 should fix, 5 could improve).
